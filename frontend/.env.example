# Leave this empty for local and LAN dev when the backend is on the same host.
# Set this only when the backend is hosted somewhere else.
# Example:
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=